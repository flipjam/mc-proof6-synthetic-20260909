# One authorized R3h startup diagnostic

Base: d4d0d448026b9f0dd0728a852d757543b550208e.
This branch has zero acceptance credit and is not a runtime candidate.
Exactly one hosted dispatch is authorized. No retry is authorized.

The sole signal_setup job retains Ubuntu 24.04, the root sudo environment
delivery, exact helper argv, pathname socket layout, native timeout/background
supervisor, custody checks, Worker ancestry/diagnostic discovery, pinned runner
audit, permission parser, IPC readiness, and qualification-only peer/reaping.
The fixed source/workflow ref is rebound to this diagnostic branch; the SHA
remains the actual dispatched SHA. There is no environment, App action/secret,
manifest, journal, isolation invocation, or acceptance operation in the workflow.
Only Statuses write is granted, with unavoidable Metadata read. Setup has no
provider request. Live serve/cleanup entry modes are disabled in this branch.

All accepted helper function/class ASTs are identical except that CUSTODY output
omits environment key names. The additional tracer observes fixed source line
IDs, not arbitrary local variables. startup-diagnostic-stage-map.json maps each
ID to the exact static source statement. It records bounded stage/error metadata,
known public runner headers, boolean token presence, numeric counts and a fixed
allowlist of possible extra system environment key names. It never prints a
token, raw job message, environment values, or arbitrary exception text.

Helper output remains in the original helper.log. Cleanup output is direct,
its exit status is captured under set +e, and an independent fixed structured
replay runs even after failed cleanup. The original nonzero cleanup status is
preserved. DIAGNOSTIC_RESULT is distinct from provider workflow conclusion.

Offline checks: all unchanged helper function ASTs compared to the exact base;
single-job permission and forbidden-surface assertions; four workflow Bash
scripts parsed by Bash; real Unix socket bind/listen and inert native helper
invocation; custody pass, expected missing local Worker failure with exact
source-stage diagnostics, bounded exception, no synthetic token in output, and
helper reaping. These checks are diagnostic preparation, not R3i verification.

The accepted live-status canary is unchanged. Held R3h runtime/journal and
provider protections must remain unchanged. No S1 rerun, freeze, or acceptance.
