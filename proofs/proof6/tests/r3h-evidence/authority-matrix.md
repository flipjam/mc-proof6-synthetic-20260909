# Proof 6 — Adversarial Acceptance Matrix

Status: PROSPECTIVE D04 STATUS-SIGNAL EXCEPTION CANDIDATE / NO IMPLEMENTATION OR EXECUTION AUTHORITY
Date: 2026-09-12
Owning repository: `flipjam/mission-control`
Source loop: `MC-CONTROL-PLANE-PROOF6-DESIGN-20260909-01`
Paired contract: `docs/AI_CONTROL_PLANE/PROOF6_ENFORCEMENT_CONTRACT_2026-09-09.md`

D04 status-signaling exception under source-loop baton 295, owner-selected Option 1, based on live main `053882ed3a75874f7e7fcdb92cc8e638035f67da`: only the paired contract's separate `D04_SIGNAL_STATUS_HELPER` exception and affected C11/D04/F07 checks change. All 72 mandatory IDs remain; the next successor requires 72 fresh, 0 inherited and 0 NOT_APPLICABLE. R3g remains terminal at F4 despite completed actual isolation: the required live ordinary-client probe was absent and D04 remains consumed. No historical credit, canary, R3h implementation, provisioning, freeze, acceptance or Phase 7 is authorized here. Independent authority review and landing precede separately authorized follow-up. Earlier amendment-stage statements below remain historical.

Recovery-append clarification under source-loop baton 237, owner disposition `AUTHORITY_CLARIFICATION_REQUIRED_WITH_CONFLICT_SAFE_TERMINAL_APPEND`, based on live main `c936c5a0582ffbafa49b0d364846e1477289633b`: only the paired contract's missing-TERMINAL completion rule and the affected recovery subconditions change. Authority-send uncertainty remains strictly non-retryable. R3e D03 remains NOT ACCEPTED, R3d remains FAILED/HISTORICAL, and no historical result receives new credit. R3f remains unimplemented/unexecuted; all 72 fresh dispositions and the accepted 56/16 accounting remain unchanged. This candidate authorizes publication for independent review only.

## Revision-3 role-boundary correction

Privilege is a predesignated owner-authorized session/environment role with bounded purpose, not a product label and not a retroactive exception. Ordinary operational clients remain non-admin and lack writer credentials; the dedicated gate-writer remains non-admin. Designated privileged setup sessions cannot supply ordinary evidence or perform or cause normal accepted transitions, even through the writer. During an accepted frozen interval they may perform read-only inspection only.

Revision-2 frozen evidence is HISTORICAL / INVALIDATED FOR ACCEPTANCE and must remain preserved. No Revision-2 result may be credited toward or combined with Revision-3 acceptance. A separately authorized new Revision-3 manifest/refreeze and interval are required. No A-H execution is authorized by this candidate. A06 and A09 remain unchanged and mandatory.

## Prospective durable-model amendment and historical boundary

Candidate for independent review under source-loop baton 223, owner disposition `AMEND_TO_DURABLE_FAIL_CLOSED_MODEL`, based on live main `34b940e0537f57e5fa225768a55214bd3d3c5340`. The paired contract's prospective durable post-send model and bounded successor delta-proof framework govern this matrix. Retain D03_REMOTE_REJECTION's genuine fixed permission rejection, all pre-send barriers, one protected journal, permanent consumption, at-most-one send and actual-process D04 isolation. Normal proposals cannot trigger faults; no arbitrary journal/write/transport/credential/network control is permitted.

All 72 mandatory IDs remain. R3d is FAILED / HISTORICAL with zero final Proof-6 acceptance credit: the owner's reported token-deletion 204 was followed by a successful authority PATCH 200 and correct protected COMMITTED reconciliation. Preserve that legitimate gate-produced roadmap-6 transition; no ordinary-client bypass occurred. Do not repair R3d in place, refund its consumed D03, roll authority back, or terminate/mutate its frozen configuration. Its successful D02 deletion/replay is regression/history only, not R3e row credit; historical R3b/R3c results also cannot discharge R3e rows.

Historical R3e authority required all 72 cases fresh and had no cross-runtime credit framework. R3e D03, run #42 / `34653255644`, remains NOT ACCEPTED / failed under that frozen authority: the original invocation did not establish required same-invocation terminal confirmation. Its preserved CONSUMED/PENDING/SEND_ARMED/TERMINAL identities are recorded in the paired contract; terminal `c3bc11b20f8bb2c55c086ebd88d930405ac7b1b8` says NOT_COMMITTED / FINAL_REJECTION and authority remained `fdf602669253e0a5d3c09f515d4dd41004db043e`. Its later visibility is not retroactive PASS or evidence that new recovery behavior was exercised.

Independent review and landing precede separately authorized R3f implementation, fresh runtime/build/plan/manifest/journal-genesis/configuration bindings and acceptance. Re-verify the actual committed baseline and rebind fixed proposals; never roll back the real roadmap-6 transition. Proof-1/2 may remain byte-identical after fresh verification. The exact R3f disposition below is 72 fresh, zero inherited and zero NOT_APPLICABLE. Historical evidence supports provenance/impact analysis only for R3f. This handoff publishes only authority documents; no implementation, synthetic/configuration mutation, provisioning, refreeze, workflow dispatch or case execution is authorized.

## Retained Revision-2 corrections

Owner-approved after bounded setup exposed two concrete issues. Revision 2 added adversarial requirements for:

1. the protected GitHub Actions writer-runtime branch/environment-secret boundary; and
2. explicit `INDETERMINATE` plus mandatory read-only reconciliation when a ref-update request may have succeeded but its response is ambiguous.

All prior mandatory bypass-resistance cases remain mandatory.

## Purpose

This matrix attempts to falsify the bounded Proof-6 claim that one exact authoritative remote Git ref cannot be mutated by ordinary clients except through the required deterministic gate-writer path.

A clean implementation test is insufficient. The proof must demonstrate actual server-side enforcement against realistic bypass paths and preserve the exact remote authority/configuration/runtime identities throughout the run.

Every case marked **MANDATORY** is required for Proof-6 PASS under its prospectively approved evidence disposition. R3f requires all 72 fresh. `BLOCKED` or `INCONCLUSIVE` on a mandatory case prevents PASS. A failed/INDETERMINATE invocation cannot be rewritten; a newly planned case may pass through demonstrated confirmed recovery only under the amended semantics, never merely because a terminal may exist.

## Evidence rule

For every mutation attempt, preserve enough raw evidence to identify:

- case ID;
- caller identity class, execution environment, and pre-action identity/permission qualification record;
- access path/tool class;
- runtime manifest identity;
- authoritative repository ID and ref;
- exact remote ref SHA immediately before;
- proposal/input when applicable;
- gate result when applicable;
- candidate commit SHA when applicable;
- whether a ref-update request was attempted;
- explicit/ambiguous remote outcome;
- reconciliation result when applicable;
- exact remote ref SHA immediately after/reconciled;
- expected versus observed result;
- writer/gate/enforcement/runtime configuration identities;
- any created commit SHA, including orphan/non-authoritative commits where relevant.

The authoritative before/after/reconciliation observation must come from the remote bound ref, not a local clone or client assertion. Bind each safety action to the exact protected journal head/record and complete genesis ancestry, operation/PENDING, plan/caller/proposal/gate/build/runtime/manifest/run identities, and disposition evidence class. Actions runs/logs/artifacts support evidence only: deletion cannot restore consumption or erase unresolved obligations. Reconciliation reads are read-only against project authority; admission requires a confirmed journal disposition, not merely a log receipt.

D03 additionally requires the paired contract's complete non-secret evidence binding: exact fixed HTTPS PATCH endpoint/method/target/candidate payload with `force=false`, operation/run/attempt/runtime/build/manifest/old/candidate, same-job read-only GITHUB_TOKEN source and independently verified effective permission boundary, transmission and response consumption, HTTP status, bounded GitHub error identity/body classification, and GitHub request ID when supplied. Qualifying rejection is actual HTTP 403 with JSON `message` exactly `Resource not accessible by integration`, consistent with the verified source, valid normal request and unchanged frozen enforcement; status/message alone are insufficient. Freeze the bounded parser/schema/classifier prospectively. Missing, mismatched, contradictory or classification-truncated evidence cannot support FINAL_REJECTION. Any live response outside that fixed permission-rejection class FAILS D03; no local/fabricated error, DNS/network failure, timeout, lost response, malformed request, rate limit, unrelated authentication failure, altered-enforcement ruleset rejection, arbitrary 4xx or 2xx may count.

## Section A — Frozen setup and authority identity

| ID | Requirement | Expected result | Class |
|---|---|---|---|
| A01 | Freeze complete runtime manifest before first adversarial attempt, including exact journal ref/genesis/schema, fixed lifecycle, proof plan, recovery subconditions, impact/hash inventory and per-case fresh/inherited disposition. | Every paired-contract identity is frozen; exactly one journal and authority ref. No implicit completion barrier or unreviewed inheritance. R3f binds all 72 fresh cases and fixed subcondition/consumption counts before execution. | MANDATORY |
| A02 | Independently resolve the synthetic repository by owner/name and immutable GitHub repository ID. | Both identify the same repository. | MANDATORY |
| A03 | Independently resolve the exact authoritative ref and baseline remote SHA. | Ref/SHA match the manifest; no alternate ref is treated as authority. | MANDATORY |
| A04 | Capture exact authoritative-ref and journal protection configurations, including allowed writer and frozen journal genesis. | Configuration demonstrably targets exact authority/journal refs and identifies allowed writer. Journal creation/recreation, deletion and non-fast-forward rewriting have no bypass, including for App; separate update restriction admits only existing writer App. Complete single-parent ancestry verifies from frozen genesis. | MANDATORY |
| A05 | Verify writer build, accepted Proof-1 reducer identity, and accepted Proof-2 gate identity before execution. | Exact identities match the manifest. | MANDATORY |
| A06 | Verify writer identity has no repository-administration/ruleset-management capability required to disable its own boundary. | Separation confirmed. | MANDATORY |
| A07 | Verify BOTH: every ordinary operational client used/classified for experiments lacks writer credential and owner/meta-admin capability and is not the gate-writer; every privileged setup/meta-authority session/environment has an explicit prior owner designation and bounded purpose, is excluded from ordinary-client evidence and normal accepted transitions, and performs only permitted read-only inspection during the frozen acceptance interval. | Identity/environment/permission qualification precedes and binds each ordinary case; no retrospective relabeling or shared-host credential leakage; privileged sessions neither perform nor cause accepted transitions, including via the writer. Separation confirmed without exposing secret material. | MANDATORY |
| A08 | Resolve exact protected writer-runtime branch, frozen commit/workflow identity, GitHub Actions environment, deployment-branch policy, concurrency identity, runner class, and workflow-token permissions. | Every runtime identity matches the manifest; runner is GitHub-hosted and environment deployment is restricted to the exact runtime branch. | MANDATORY |
| A09 | Verify the App private key is bound only as the declared GitHub Actions environment secret and is absent from repository content, artifacts, logs, ordinary-client/local storage, alternate branches, and ordinary workflow inputs. | Isolation confirmed without disclosing the secret or recoverable derivative. | MANDATORY |

## Section B — Direct ordinary-client bypass attempts

These cases must use prequalified ordinary non-admin, non-writer credentials with meaningful repository write authorization; rejections solely for missing write access are insufficient. Qualification must bind the actual caller/environment/path to each case before action. Privileged setup/owner credentials cannot count, even if the server rejects them. No failed ordinary case can be excused by retrospective privileged relabeling. Where a surface is unavailable for the synthetic repository, Mission Control must disposition the case before execution; the implementation team may not silently omit it.

| ID | Attack | Expected result | Class |
|---|---|---|---|
| B01 | Normal `git push` directly updates authoritative ref. | Server rejects; remote authority SHA unchanged. | MANDATORY |
| B02 | Ordinary non-admin repository writer attempts Git-refs API updates of authority and protected journal. | Both rejected; bound SHAs unchanged; no arbitrary journal append path. | MANDATORY |
| B03 | GitHub Contents API attempts a file write targeting authoritative ref. | Rejected; SHA unchanged. | MANDATORY |
| B04 | GitHub UI direct file edit targeting authoritative branch/ref. | Cannot advance authority; SHA unchanged. | MANDATORY |
| B05 | Pull request / merge path attempts to land an ordinary-client commit into authoritative ref. | Cannot advance authority; SHA unchanged. | MANDATORY |
| B06 | ChatGPT-accessible GitHub write path attempts a direct authority update instead of gate-writer operation. | Cannot advance authority; SHA unchanged. | MANDATORY |
| B07 | Codex/local Git credentials attempt direct authority update instead of gate-writer operation. | Cannot advance authority; SHA unchanged. | MANDATORY |
| B08 | Ordinary credential attempts force-push/ref rewrite of authority and protected journal. | Rejected; both bound SHAs unchanged. | MANDATORY |
| B09 | Ordinary credential attempts deletion of authority and protected journal. | Rejected; both refs remain at same SHAs; frozen creation/recreation prohibition independently verified under A04. | MANDATORY |
| B10 | Ordinary credential creates/modifies another branch containing plausible replacement authority or lookalike journal. | Neither substitutes for either exact bound ref; forged consumption/disposition cannot authorize work. | MANDATORY |
| B11 | Ordinary credential writes a commit that changes only an ostensibly “unprotected” path and tries to advance authoritative ref. | Ref update is rejected regardless of changed path. | MANDATORY |
| B12 | Ordinary client attempts a merge/push path that skips any CI/status-check execution. | Lack/skipping of CI does not enable authority mutation. | MANDATORY |

## Section C — Gate-writer positive and negative behavior

| ID | Experiment | Expected result | Class |
|---|---|---|---|
| C01 | Submit one currently ALLOWed proposal through the protected writer; inspect journal ordering and fresh admission after confirmed recovery. | Confirmed bound PENDING and SEND_ARMED/equivalent precede transport; exactly one non-force fast-forward update of the exact gate-produced candidate with no unrelated authoritative changes. Confirmed durable disposition precedes later authority mutation; recovery never supplies a reusable old send permit. | MANDATORY |
| C02 | Submit proposal that gate deterministically REJECTs. | No authority mutation. | MANDATORY |
| C03 | Submit malformed proposal. | Rejected/error; no authority mutation. | MANDATORY |
| C04 | Submit stale expected-state proposal after a successful authority advance. | Rejected; no additional authority mutation. | MANDATORY |
| C05 | Replay a previously incorporated proposal/candidate. | Rejected according to accepted gate semantics; no mutation. | MANDATORY |
| C06 | Attempt alternate repository, authority/journal ref or arbitrary journal record through request/workflow tricks. | Unsupported/invalid; exactly bound targets remain fixed; no other ref/file mutation authorized. | MANDATORY |
| C07 | Attempt to supply arbitrary authoritative file/history bytes rather than a transition proposal. | No public raw-write operation exists; authority unchanged. | MANDATORY |
| C08 | Attempt to supply arbitrary parent SHA/commit identity. | Writer ignores/rejects caller control; uses freshly read current authority. | MANDATORY |
| C09 | Inspect successful committed diff/history. | Only exact gate-produced authoritative candidate mutation is present. | MANDATORY |
| C10 | Inspect writer public/internal mutation paths for arbitrary Git/ref/file/journal write escape hatch. | Public raw writes absent; internal journal only fixed-schema safety records at frozen paths. Journal grants no second project-state authority or arbitrary App mutation permission. | MANDATORY |
| C11 | Inspect workflow inputs, effective ordinary GITHUB_TOKEN permissions/source and proof-operation consumption; challenge write-capable/mismatched/missing/invalid job token, App-token fallback and replay after Actions evidence deletion. If used, verify every paired-contract D04_SIGNAL_STATUS_HELPER condition, including source review plus bounded nonsecret runtime tests/inspection of custody in both directions: environment/argv exposure, inherited descriptors, IPC, implementation-created/shared credential files/stores, implementation-defined inspection/retrieval or other credential-delivery paths, and receipt/use by the wrong process. Challenge broader scopes and forged/stale/cross-run events. | Normal proposal cannot select faults. Only frozen D02/D03/D07 combinations and separate D04 operation; confirmed permanent CONSUMED required. Failed/crashed/deleted runs never refund authorization; replay/mismatch fails closed. Neither interface permits command/repository/ref/workflow/environment/runner selection or token-source/permission/credential/endpoint/payload/force control. The actual writer's ordinary GITHUB_TOKEN remains read-only and incapable of an accepted project transition; only D03's one fixed rejection-attempt PATCH may use that same-job token. Unverified/mismatched/write-capable/missing/invalid writer job token fails closed before transport; App substitution/fallback cannot pass. Successful job-token mutation FAILS D03; the rejected proof request grants no writer role. Only the dedicated App is authorized for accepted authority mutations. The sole status-write exception is the separate fixed D04 helper with prospectively bound target/context/IPC/events, narrow effective permissions and verified custody/noninterference. No broader write scope, implemented-path delivery/exposure/retrieval of the helper token to the writer or App credential to the helper, caller-selected status input or status-based authority decision is allowed. Apply the paired contract's execution-path custody boundary, not secrecy against arbitrary hostile-root introspection; root capability or a promise not to use a received credential never excuses cross-delivery or broad availability. Missing qualification blocks use; the exception never applies to D03 or normal work. | MANDATORY |

## Section D — Stale authorization, concurrency, ambiguity, and failure handling

| ID | Experiment | Expected result | Class |
|---|---|---|---|
| D01 | Two requests concurrently target the same starting authority and are independently valid only from that same state. | Protected workflow concurrency serializes mutation jobs. At most one candidate from the old SHA can commit; the later job re-reads/re-evaluates and cannot reuse stale authorization. | MANDATORY |
| D02 | Exercise consumed D02 after fresh gate ALLOW/candidate construction, before any authority transport entry. | Confirmed journal NO_SEND/terminal safety record binds candidate/object identities and update_attempted=false; authority unchanged. Missing proof cannot establish no-send; possible transport entry forbids false pre-send reporting. | MANDATORY |
| D03 | Exercise D03_REMOTE_REJECTION with qualified caller, confirmed permanent CONSUMED, fresh reconstruction/gate ALLOW/candidate, confirmed PENDING then SEND_ARMED, fixed read-only same-job GITHUB_TOKEN, exactly one normal fixed authority PATCH with force=false. R3f must exercise the fixed post-terminal confirmation-loss/recovery subcondition. | Genuine bound GitHub PERMISSION rejection plus fresh authority-old check precedes TERMINAL NOT_COMMITTED / FINAL_REJECTION. A: original invocation appends and confirms; or B: it appends but cannot confirm, remains INDETERMINATE, never retries, and admission blocks until later qualified recovery validates complete journal+authority and durably confirms the exact disposition. B requires demonstrated recovery, not possible terminal existence. App retains reads/objects/journal but never supplies D03 PATCH credential. No second PATCH/fallback/force/rebase/merge. Unexpected success FAILS; observed candidate is real COMMITTED, never rollback. Existing contradictory terminal -> BLOCK/escalation, no overwrite. R3e historical verdict unchanged. | MANDATORY |
| D04 | After permanent CONSUMED, isolate the ACTUAL runtime process normally hosting authority-writing behavior from api.github.com for a frozen window of at most 120 seconds; independent ordinary client remains connected and attempts meaningful direct authority mutation. If using D04_SIGNAL_STATUS_HELPER, verify its pre-consumption readiness and live READY/END sequence under the paired contract; challenge startup failure, early/absent/stale/forged signals, timeout and orphan helper. | Same job holds sole writer concurrency boundary; no authority send or alternate writer. Direct attempt rejected, authority unchanged, fixed START/READY/END and restoration evidence. Helper/probe-only isolation cannot pass; failed real isolation BLOCKS. No arbitrary network controls or enforcement modification; absent completion loses credit, never refunds consumption. Provider READY must be observed while the job is in_progress and bound to this run/attempt/runtime/target/context; ordinary PATCH follows observed and actual READY and precedes actual END. Actual isolation/predicates, frozen 30-second interval, fresh END, provider END, successful supervisor completion and helper reaping remain required. Status alone proves none of them. Checkable helper failure blocks before consumption; later failure never refunds/retries D04. | MANDATORY |
| D05 | Present a formerly valid ALLOW result/token/output to the writer after authority has changed. | Old authorization cannot be committed without fresh read and gate evaluation. | MANDATORY |
| D06 | Retry legitimate later work after a known pre-update transient failure; also attempt to repeat consumed proof behavior. | Fresh read/gate only after required journal disposition; stale/uncertain candidate never resumed/resubmitted. Consumed proof operation remains spent even after failure/crash or Actions evidence deletion. | MANDATORY |
| D07 | Consume fixed D07, confirm protected PENDING, transmit one genuine normal PATCH and lose response before authoritative result consumption; remove supporting Actions evidence for unresolved send. | INDETERMINATE binds exact old/candidate, update_attempted=true and unknown outcome; no resend or known-success relabeling. Protected PENDING survives process/run/log/artifact loss and remains admission obligation. | MANDATORY |
| D08 | Reconcile complete canonical journal plus current authority, including an unconfirmed or missing terminal after D07; challenge old-only release, disposition contradictions and same-parent completion siblings. | Exact binding/schema/ancestry/affirmative evidence/current-authority consistency required even for a record marked resolved. Candidate may prove COMMITTED but contradicting NOT_COMMITTED terminal BLOCKS/escalates. Old permits NOT_COMMITTED only with affirmative exact no-send/final rejection or prospectively proven barrier; old alone BLOCKS. Existing valid canonical terminal makes recovery for O read-only. Missing-terminal completion requires all paired-contract O/P/D preconditions, identical safety semantics, direct child of P, force=false and one PATCH per recovery invocation; only canonical reread can resolve. Invalid canonical winner BLOCKS without corrective append. Never duplicate canonical disposition, overwrite or resend authority candidate. Validate complete explained authority continuity after older dispositions; no arbitrary-descendant shortcut. | MANDATORY |
| D09 | Exercise original terminal append followed by confirmation failure/crash/partition, later recovery and attempted new work; challenge missing/stale/malformed/wrong-operation/noncanonical journal/terminal and conflict-safe completion, including delayed-first/later-completion ordering. | Original invocation remains INDETERMINATE after unconfirmed post-send disposition. Complete protected replay, exact evidence and authority checks, serialized durable reconciliation and final head/authority verification precede fresh admission. H0/H1 permit the same one-shot TERMINAL completion only while exact unresolved O/P/D preconditions hold; no in-process retry or new completion after a valid canonical winner. Live same-P sibling evidence must show one canonical winner and rejected replacement, with zero additional authority PATCHes. Unreadable/unexpected/contradictory canonical state BLOCKS; local flags/logs/2xx cannot release. Pre-send PENDING/SEND_ARMED confirmation, permanent consumption and no authority retry remain strict. Recovery cannot mint a send permit or bypass caller/security checks. | MANDATORY |

D03/C11 include these mandatory adversarial expectations within their existing IDs; bind their verification method/evidence in the successor proof plan without adding a mode, refunding consumption or repeating the once-only live D03 operation:

- Write-capable GITHUB_TOKEN is disallowed except for the fully qualified, separately isolated D04_SIGNAL_STATUS_HELPER credential; that exception never applies to D03. Unverified permissions, credential-source mismatch, caller-selected token/scope or missing/invalid token always fail closed before transport, including for the helper; broader helper scopes cannot pass. A token variable name or declared read permission alone is not effective-permission/source proof. App fallback or a second PATCH defeats the case even if GitHub rejects it.
- An unrelated 403, local/fabricated error, missing/mismatched response, or any other nonqualifying response cannot establish successful D03 or FINAL_REJECTION. Lost/ambiguous response retains the protected send obligation; old SHA alone never releases it.
- Unexpected HTTP 200 or any 2xx FAILS D03. Exact candidate observation requires COMMITTED; contradictory or unreadable state requires INCONSISTENT/BLOCKED. Never roll back or append NOT_COMMITTED for an observed commit.
- Terminal append failure/crash after SEND_ARMED must leave later admission blocked until exact admissible evidence supports a remotely confirmed disposition. Timeout, repeated reads, runner exit and token expiry do not release ambiguity. Natural/D07 retains its genuine ambiguity/evidence requirements and uses the same prospective durable terminal/recovery model as D08/D09.

### Required fail-closed subconditions within existing cases

These are subconditions of the existing mandatory IDs, not additional numbered cases or permission to mutate frozen protected state. Bind exact execution methods/fixtures before R3f execution. The D03 confirmation-loss/recovery path must exercise the real successor path after one genuine fixed GitHub rejection; negative malformed/contradictory cases may use isolated fixtures on the same production validators/admission code. Fixtures never count as genuine remote enforcement evidence. A method that would require a second live consumed D03, rewriting a protected record or widening a fault surface must return for review instead.

| Subcondition | Mandatory rows | Required result |
|---|---|---|
| Terminal PATCH succeeded; confirmation read failed | D03, D09 | Original invocation INDETERMINATE; no retry/second authority PATCH; later mutation blocked pending complete confirmed recovery. |
| Terminal PATCH failed or outcome unknown | D08, D09 | Do not infer whether an earlier PATCH was sent. H0/H1 permit one conflict-safe completion per later recovery invocation only under every exact O/P/D precondition; no in-process retry. Missing evidence, unreadable/unexpected head or invalid winner BLOCKS; no release without canonical reread. This grants no authority or other journal-write retry. |
| Stale read after terminal append | D08, D09 | Stale prefix/head cannot erase SEND_ARMED or prove confirmation; no release until fresh complete canonical evidence and authority consistency are established. Repeated old reads are no completion proof. |
| Malformed or wrong-operation terminal | D08, D09 | BLOCK; no ignore/skip/repair. Exact operation/PENDING/SEND_ARMED and evidence schema must match. |
| Terminal outside canonical ancestry | E03, D08, D09 | Reject it as disposition authority; detached object, losing sibling, local file or lookalike ref never resolves O. An orphan alone does not invalidate a fully validated canonical winner or authorize repair; absent canonical resolution, admission remains BLOCKED pending admissible completion. |
| Crash immediately after terminal creation | D03, D09 | No resolved success; preserve consumption and possible-send obligation. Object creation alone is insufficient; later recovery verifies canonical reachability. |
| Network partition after append | D03, D09 | INDETERMINATE and blocked admission; restored connectivity alone grants no release. |
| Later invocation sees valid terminal | D03, D08, D09, C01 | Recovery for O is read-only, including D03's already-canonical terminal. Complete replay and exact evidence/current-authority checks, durable confirmation and only-then fresh gated work. No blind resolved-record skip, new completion PATCH or duplicate terminal append. |
| Later invocation cannot read journal | D09 | BLOCK before gate/send; cached history and Actions discovery cannot substitute. |
| Malicious ordinary resend | C05, C11, D06 | Reject; consumption not refunded by failure/deleted logs/new attempt; old candidate never resumed. |
| Duplicate writer send | C01, D01, D07, D09 | Reject before transport; same invocation and recovery cannot reuse/reconstruct a consumed send permit. |
| Authority advanced despite NOT_COMMITTED evidence | D08, D09, H02 | BLOCK/escalate unless complete canonical history proves independently fresh legitimate later transitions after confirmed disposition. Mere descendant/new SHA is insufficient. |
| Terminal NOT_COMMITTED but candidate actually committed | D03, D08, D09 | Contradiction BLOCKS/escalates; preserve real commit and conflicting record, no rollback/overwrite/false rejection. |
| Excluded meta-authority changes protected state | G03, F03, H07 | Invalidate interval even if restored. Recovery is no admin-repair exception; retain history and require new authorized boundary. |

### Conflict-safe TERMINAL completion proof subconditions

The paired contract's exact completion constraints apply to every row below. These are subconditions under existing D07/D08/D09 and H07, not new numbered cases. Preserve the once-only D07 authority send and D03's read-only confirmation of an already canonical valid terminal. Freeze methods within the existing R3f recovery proof budget; no automatic additional workflow, fault mode or eleventh run is prescribed.

| Subcondition | Mandatory rows | Required evidence/result |
|---|---|---|
| H0/H1 and same-P equivalent candidates | D07, D08, D09 | After the genuine authority ambiguity, exact protected inputs admit one D(O). Two distinct Git commit objects are direct siblings of the same SEND_ARMED parent P, with identical operation/lifecycle/disposition/affirmative-evidence/parent/required-authority bindings. Only metadata with no safety semantics may differ; no deterministic commit identity or recovery-attempt record is required. |
| Live protected journal winner/loser | D08, D09, H07 | At least one bounded LIVE GitHub demonstration on the actual protected journal: both requests admitted under valid unresolved preconditions at P, each invocation issues at most one force=false completion PATCH; only one sibling becomes canonical and the loser cannot replace it. Complete canonical reread establishes the winner; 2xx alone does not. An orphan has zero disposition authority. Zero additional authority mutation. Offline simulation alone is insufficient. |
| Delayed-first/later-completion ordering | D08, D09 | A completion admitted at P may arrive after another admissible completion. Different sibling cannot fast-forward over winner or later legitimate descendants. No new completion is admitted after observing a valid winner. Unknown confirmation retains INDETERMINATE/BLOCKED until complete canonical reconstruction; no Actions/log membership assumption. |
| Invalid canonical winner or changed semantic field | D08, D09, H07 | Same-validator negative fixtures show malformed/wrong-operation/wrong-disposition/evidence/lineage/authority inconsistency BLOCKS. No better sibling, overwrite, skip, reinterpretation or admission release. Changed safety fields cannot be ignored as metadata. Do not corrupt the live journal to create this fixture. |
| Strict exception boundary and future read-only recovery | D08, D09, H07 | Repeated recovery never sends another authority PATCH, resumes stale gate output or refunds consumption. Valid canonical terminal makes future recovery for O read-only after full checks. Caller cannot choose O/P/D/content/target. No repeat-attempt permission for CONSUMED/PENDING/SEND_ARMED, arbitrary records, other refs or side effects; no force or in-process retry. |

## Section E — Authority-confusion attacks

| ID | Attack | Expected result | Class |
|---|---|---|---|
| E01 | Modify only a local clone/worktree and claim new state. | Remote authoritative ref/read remains unchanged. | MANDATORY |
| E02 | Push valid-looking history to alternate branch and claim it is authority. | Control-plane reads continue using only manifest-bound ref. | MANDATORY |
| E03 | Create tag/similarly named ref with alternate state or journal records; claim journal appends advance project state. | Only bound proof6-authority determines project state. Exactly bound journal supplies safety membership only; lookalikes are ignored. | MANDATORY |
| E04 | Create PR/task branch with newer commit than authority and claim newest commit wins. | Publication does not equal authorization; authority remains the bound ref. | MANDATORY |
| E05 | Make `proof6-writer-runtime` or another unrelated branch the repository default branch. | Authority interpretation remains tied to exact bound `proof6-authority` ref; default branch does not become authority. | MANDATORY |
| E06 | Supply stale/cached local authority to gate-writer request. | Writer independently fetches current remote authority and evaluates against it. | MANDATORY |

## Section F — Enforcement, runtime, and credential integrity

| ID | Attack/inspection | Expected result | Class |
|---|---|---|---|
| F01 | Ordinary client attempts to disable/modify authoritative-ref enforcement/ruleset configuration. | Not permitted. | MANDATORY |
| F02 | Gate-writer identity attempts to disable/modify authoritative-ref enforcement/ruleset configuration. | Not permitted by its granted capability. | MANDATORY |
| F03 | Compare frozen authority and journal enforcement configuration before/after normal cases. | Same relevant configurations; legitimate journal appends do not alter frozen genesis/schema/protection identities. | MANDATORY |
| F04 | Compare frozen writer build/gate/reducer identities before and after run. | Unchanged. | MANDATORY |
| F05 | Examine whether PASS relies on a CI check that a writer can skip. | PASS rests on server-side authority-write restriction; GitHub Actions is the isolated writer runtime, not the sole authority-ref enforcement boundary. | MANDATORY |
| F06 | Examine whether push and merge permissions differ in a way that permits one bypass. | Neither ordinary path can advance authority. | MANDATORY |
| F07 | Examine service credential placement/scopes and effective two-ref mutation restrictions. If using D04_SIGNAL_STATUS_HELPER, independently refresh status noninterference before separately authorized canary and freeze, including all status surfaces reachable by its repository-scoped credential, required checks/rulesets, environment/deployment policy, workflow triggers and authority consumers. | Writer secret remains isolated from ordinary clients; permissions no broader than required. For authority/journal writes, existing non-admin App only; no new credential class/admin/ruleset capability. Only fixed gate-produced authority updates and fixed journal records; no arbitrary refs/files or runtime mutation. The separately isolated D04 helper has only statuses write (including status read) and unavoidable metadata read; statuses cannot affect protected-state mutation/admission, trigger privileged work or supply gate/journal/recovery authority. Unproved noninterference blocks use; changes during acceptance invalidate the interval. Repository contents permission alone does not prove this boundary. | MANDATORY |
| F08 | Attempt direct use of any ordinary alternate credential available to ChatGPT/Codex/human client. | No such ordinary credential can advance authority. | MANDATORY |
| F09 | Ordinary client attempts to update/delete/rewrite the protected writer-runtime branch. | Server rejects; frozen runtime-branch SHA unchanged. | MANDATORY |
| F10 | Gate-writer App attempts to update the writer-runtime branch. | Server rejects; App has no runtime-branch bypass. | MANDATORY |
| F11 | Attempt to run the secret-bearing writer job from an alternate/unprotected branch or alter the environment deployment branch. | Environment secret is not released outside the exact frozen runtime branch; policy change would invalidate the run. | MANDATORY |
| F12 | Inspect runner class and workflow execution environment. | Writer mutation job uses GitHub-hosted runner only; no self-hosted/local/WSL/Docker runner within ordinary-client admin reach. | MANDATORY |
| F13 | Inspect workflow dependencies/actions. | Any external action is pinned to immutable commit identity; no unpinned third-party code executes with the App secret. | MANDATORY |
| F14 | Examine environment secret exposure through logs/artifacts/outputs/debugging. | No key, installation token, JWT, or recoverable secret material is emitted or persisted in proof evidence. | MANDATORY |

Compromise or theft of the dedicated writer credential inside the trusted GitHub-hosted runtime is outside the bounded Proof-6 threat claim; however, failure to isolate that credential from ordinary clients, runtime-branch substitution, or secret release from an unapproved branch is a Proof-6 FAIL.

## Section G — Owner break-glass / meta-authority

| ID | Inspection | Expected result | Class |
|---|---|---|---|
| G01 | Verify distinct owner/meta-admin authority, including each designated privileged setup session/environment, is identified before action/evidence with bounded owner-authorized purpose and is not an ordinary operational client or the gate-writer. | Separation documented; designation is prospective and session-specific, never a global ChatGPT/Codex exemption. | MANDATORY |
| G02 | Verify privileged setup/meta-authority neither performs nor causes any normal accepted authority transition (including via the writer), supplies ordinary-client evidence, nor makes a bypass test pass. | None does; admin/writer credential transfer to ordinary clients violates mandatory separation. | MANDATORY |
| G03 | Check for any privileged mutation of authority, operation journal, enforcement/rulesets, runtime/workflow/build, environment/credential binding, or manifest artifact/variable during the accepted frozen interval; only read-only privileged inspection is permitted. | None occurred. Any such mutation invalidates the interval even if restored, cannot count as a gated transition, and requires a fresh manifest/run. | MANDATORY |

Proof 6 does not require exercising destructive break-glass. Production recovery/reconciliation semantics remain deferred beyond the bounded indeterminate-outcome rule in the paired contract.

## Section H — Independent review and evidence closure

| ID | Requirement | Expected result | Class |
|---|---|---|---|
| H01 | Independent reviewer re-fetches final authoritative remote ref and relevant evidence from exact published identities. | Final authority and evidence are reproducible without conversational memory. | MANDATORY |
| H02 | Reviewer maps every successful authority advance to one gate-writer request and deterministic gate ALLOW. | One-to-one mapping; no unexplained authority mutation. | MANDATORY |
| H03 | Reviewer maps every direct-write/bypass attempt to unchanged authority ref. | Complete mapping. | MANDATORY |
| H04 | Reviewer checks every mandatory ID, prospective impact/hash analysis, selected fresh cases and any inherited evidence against exact frozen authority. | No silent omission/reclassification or retroactive credit. All 72 R3f rows fresh; later inheritance only under the contract's independently approved eligibility conjunction. Full-rerun triggers cannot be waived by plan, canary or implementer judgment. | MANDATORY |
| H05 | Reviewer attempts at least one fresh bypass path not authored by the implementer, within the frozen threat boundary. | No bypass found; if bypass succeeds, FAIL. | MANDATORY |
| H06 | Reviewer confirms main real-project repositories were not made authoritative proof subjects. | Proof remained synthetic/bounded; no pilot/migration/adoption occurred. | MANDATORY |
| H07 | Independently inspect complete protected journal and every possible authority send/ambiguity, including original confirmation failure, later recovery, deleted supporting evidence and the live conflict-safe TERMINAL completion demonstration. | Every authority send has prior confirmed PENDING/SEND_ARMED; consumption survives. Equivalent direct siblings of exact P yield only one canonical disposition; no losing-sibling replacement, orphan credit, invalid-winner repair or additional authority PATCH. Every obligation has valid confirmed disposition and explained current-authority continuity. No unresolved/contradictory history, old-only or malformed/noncanonical release. Original INDETERMINATE report remains historical; case credit requires actual prospective recovery and live provider evidence. | MANDATORY |
| H08 | Reviewer independently resolves the frozen runtime-branch/workflow/environment policy identities. | They match the manifest and remained unchanged during accepted evidence collection. | MANDATORY |

## Exact prospective R3f case disposition and impact basis

This table is normative for the current R3f plan: **72 fresh evidence dispositions; 56 FRESH_R3F and 16 NO_VALID_PRIOR_EVIDENCE_SO_FRESH reason categories; 0 inherited; 0 NOT_APPLICABLE**. Each entry refers to the same existing mandatory ID above. The reviewed accounting under source-loop baton 227 separates actual direct/transitive change, successor security/configuration and mandatory fresh review/canaries from lack of qualified historical acceptance evidence. No row relies on a blanket shared-admission full-rerun rule; the table does not claim all 72 are behaviorally coupled. Counts are not an optimization goal, and this correction authorizes no reduction.

### Retained source/evidence identities inspected read-only

Synthetic source repository for these immutable commits: `flipjam/mc-proof6-synthetic-20260909`. Manifest/evidence paths below are in `flipjam/mission-control` at the exact evidence commits, not mutable task labels.

| Interval | Frozen runtime source | Mission Control retained evidence commit | Manifest path | Exact manifest file SHA-256 |
|---|---|---|---|---|
| R3b | `46d2400269120c26015bc2863ca14ac2c6b02d16` | `73a605c9d75b3d4ccca4d596b4e9cb889a249616` | `proofs/proof6/r3b/PROOF6_R3B_FROZEN_MANIFEST.json` | `57f670e058647a2da964e56f78c6969aca234406e10a643f2fe9384daff1f011` |
| R3c | `a58413651ffbaa77ef26dd35bdda78a649d4d22e` | `271b22c190aa5cc8f1c4086ff3fac2ba89a741bc` | `proofs/proof6/r3c/PROOF6_R3C_FROZEN_MANIFEST.json` | `ee00b9347cfaceda35db5ce5c35d15b0e3028c058f472657a1299541011c1ebc` |
| R3d | `35c3ea5eff822dc6e4dc1e8f5e0b6f76f0d632e5` | `d34f918e809914fe18f470b3a240b4270fce1ec2` | `proofs/proof6/r3d-freeze-v3-resume/PROOF6_R3D_FROZEN_MANIFEST.json` | `17a1b9c0d7f56da9b4b544f2fbe329fdf5bbe46353ddc6b581b9a5c9e6c9b72a` |
| R3e | `d1a8b2fcbd5ffc3aec8f02823bb604dce542f06f` | `83b94c220243fe5f62d090c25807e772cf14772e` | `proofs/proof6/r3e-freeze/PROOF6_R3E_FROZEN_MANIFEST.json` | `a4447582744c5eb0d500f3ad7c7a4d35d07e1220433c417ae57418269475574c` |

Relevant retained evidence and limits:

- R3b: `R3B_TERMINAL_SNAPSHOT.json` under `proofs/proof6/r3c/` at the R3c evidence commit retains actual receipts, including run #26 / `34536509735` with proposal `proof6-r3b-c01` COMMITTED, plus rejected/stale/error receipts. These are real historical observations, not a claim that all rows passed. R3b/R3c admission depends on Actions run/log discovery; their identical `reconcile.disposition` treats remote-old as NOT_COMMITTED. Neither source implements the successor protected-journal/durable-confirmation model.
- R3c: `R3C_REJECTED_PRESERVATION.json` under `proofs/proof6/r3d-freeze-v3-resume/` at the R3d evidence commit records REJECTED/HISTORICAL and a latest setup-only #36 / `34547470950` receipt. Setup is not acceptance. The frozen plan is not proof that a planned case ran.
- R3d: canonical journal head `d831cb125a0c91b4d2b36ba0d063cba6508b37e3` was read through genesis `ab3a6e083fb05d6ed4bce6a8442fab57378018ac`. D02 run `34621864118` has an UNARMED/NOT_COMMITTED terminal `aa6781ed0e9cb7aff2e3f86bcae7f9b0558e64ee`; D03 run `34624583736` ends COMMITTED/CANDIDATE_OBSERVED at that journal head. D02 deletion/replay remains historical regression value, not successor case credit; D03 is failed. The old revocation/retained-log parser is materially different from R3e's receipt validator.
- R3e: run #42 / `34653255644`, attempt 1, is recorded failed at the pinned runtime. Canonical journal head `c3bc11b20f8bb2c55c086ebd88d930405ac7b1b8` was read through genesis `e5ee3f9f8416a669be334f3aab67653a09635dd6`: only the four D03 lifecycle records follow genesis. Terminal blob `26991b3a719c3282ef9754c8940e9a29fa5e6278`, SHA-256 `d8f54e332fa84bfed6b34504b10cc3b8a6e2a0e0b3449025ac1324fd21abdf08`, carries the exact 403 permission-rejection receipt, old/candidate, framing/consumption and credential bindings. This does not satisfy the historical missing same-invocation confirmation. The freeze manifest's zero cases is a setup-time snapshot, not a post-run inventory. Journal presence does not establish other acceptance rows; no nonexistent R3e evidence is inherited.

Even a separately valid older bypass probe cannot attest today's mutable grants/enforcement/environment. No row is currently proven inheritable: the 56 rows below require fresh evidence for their stated impact/security/review reasons, while the other 16 lack a sufficiently qualified, independently reviewed prior acceptance package. Historical observations and unchanged source may support provenance/impact analysis without establishing case acceptance or accepting a failed interval.

### Source dependency and configuration impact

At R3e's exact source identity, validated requests reaching `writer._Writer.commit_transition`'s journal lifecycle call `Journal.recover` before consumption, fresh authority read/gate and normal/D02/D03/D07 send paths. Malformed proposal parsing/validation and some unsupported input checks reject before `Journal.recover`; not every input path reaches recovery. `actions_runtime.main` also calls recovery before D04 consumption, but that shared precheck alone does not establish behavioral impact on the D04 outage claim. `Journal.read -> validate` owns canonical lifecycle state, `finish -> append` owns terminal persistence/confirmation, and `arm -> take_send` owns the one-shot permit. `reconcile.py` rejects standalone reconciliation; `admission.py` is only receipt formatting. Consequently unchanged wrapper bytes do not establish unchanged admission.

Inspected R3e `Journal.recover` skips a pending operation once it is in `resolved`; the current-authority callback is reached only for unresolved entries. The amendment instead requires current-authority consistency/complete continuity even for an already present valid terminal before new admission. R3e `writer.py` assigns D03 PASS only after `finish` returns; a failed return leaves its original evidence INDETERMINATE. These are source dependency observations, not a live exploit or new acceptance execution. D03/D07/D08/D09 directly exercise changed recovery/ambiguity behavior; the table separately identifies affected admission, duplicate-send, consumption and journal-validation claims. It does not classify C01-C11 as uniformly coupled: some malformed/unsupported paths reject before recovery, and many B-family bypass probes do not execute recovery at all. C03/C07/C08, D04/D05 and the other reviewed evidence-gap rows are fresh because qualified prior acceptance packages were not established, not because a common function or precheck proves impact on their behavior.

The exact retained source comparisons show:

- R3b -> R3c: workflow, launcher, admission, build, diagnostic bindings and writer change; outage/proof control/plan are added; reducer, gate, diagnostics, reconcile and ruleset view are unchanged.
- R3c -> R3d: workflow, launcher, admission, build, outage, proof control/plan, reconcile and writer change; journal and D03 rejection recovery are added; reducer, gate, diagnostic bindings/diagnostics and ruleset view are unchanged.
- R3d -> R3e: workflow, launcher, build, D03 rejection validator, journal, proof control/plan and writer change; job-token verifier is added. Reducer, gate, admission wrapper, diagnostic bindings/diagnostics, outage, reconcile stub and ruleset view are unchanged. No files are removed in these three comparisons.
- Historical runtime-ref and deployment-branch bindings differ; R3b/R3c have no protected-journal manifest binding, whereas R3d/R3e have distinct journal refs/genesis/schema/protection. D03 also changes credential/transport/evidence from revocation to fixed job-token rejection. Identical reducer/gate bytes cannot bridge these security/admission changes.

The following full R3e file inventory is a reproducible baseline for the future exact R3f diff, not a fabricated R3f build. Immutable earlier `proofs/proof6/build.json` files bind their dependency hashes; their own SHA-256 identities are R3b `c4e3cd62d37c0b7e631874f324ddbbb9968eae27d07fbc2301c9c8267d0de164`, R3c `1efc5f7db0db162f984946b5bd5cadb085833bb40fabe8b8be0ce514b7d2c111`, R3d `d9b0aab7fdbb3f3db2983864706eaa7c553204885974efb38263e7f1e028bba5`. No R3f implementation exists yet; the current 72-fresh selection rests on accepted prospective semantics, known successor/security obligations and currently established historical evidence. Future exact implementation review must supply changed AND unchanged hashes, the full source/build/runtime/configuration diff, transitive impact verification and proof-method bindings before freeze. This correction preserves the 72-case selection. Any proposed disposition change requires independent approval and explicit Mission Control authorization before acceptance execution; authors cannot reduce a frozen set after seeing results.

| R3e source path | Git blob | SHA-256 |
|---|---|---|
| `.github/workflows/proof6-writer.yml` | `0d88a4ae6db850261d42f3991788390778b1b3aa` | `6f94a6b91cc95b470a0f3e8ae68e4ffde7b35461ce5d9a56b75ba89109120163` |
| `proofs/proof1/replay.py` | `efdfb8fbcd293e006709ca386d337814ce19683c` | `ac9c761cf57d74ef68f35213de333c2d071c1f030ad592cae444c3bee576474e` |
| `proofs/proof2/gate.py` | `1866724af1908053491b945a902a3d0038abca5d` | `e432172177d7d58d1b9424c4547f31e7223295eeb80d0a3235bd2e7092ca60bf` |
| `proofs/proof6/actions_runtime.py` | `586ff9bf024e8265a72416722d901a51e2a42b12` | `61bee197bf26f54457371cca8c6fe29a425705a7c5c6548ec439de2d5dba7374` |
| `proofs/proof6/admission.py` | `5e99cd5c1fcf66af7dcb4c8e568449d09b78cbd9` | `5c94385aada6d7dbfe25b3e014363ded818ec795607d929314c2000a84224469` |
| `proofs/proof6/build.json` | `2ff4880c304154f7e014e0928782ca67fa2d252c` | `fea1668b0922f9ab29ec97d17df10327b118d741b58b8b6eaa2153b02fa3ac8c` |
| `proofs/proof6/d03_job_token.py` | `3935ba8b1284e6b56d2f0870be4449424bdbff68` | `3a6af044d0b09845924a0c5b2bb3bdd7bfbd5c9970e35b3346ff198e47cfe5ae` |
| `proofs/proof6/d03_rejection.py` | `1ac498b5c67d7f6690f2bdbf421af954daa07db9` | `79af705e8b0f405d4ceda4bd12b4550dea9d0cb2cb2330252baf6e40c1132db1` |
| `proofs/proof6/diagnostic-bindings.json` | `3939cb799d20cedf5842ac60073bdd86543eb874` | `19f86f39bd70aa8f996465726654a659b824196e5ec155c0965d50f2eb5f2f4a` |
| `proofs/proof6/diagnostics.py` | `455ab4048ef28a5ec157c5deb6896f32b8bb3afc` | `54f8aefa6c8c1914e01d39410b28945c3d5b38bc5694d34e3f3b61064b9aa934` |
| `proofs/proof6/journal.py` | `826a072c360264f85b960576ddb79923d158e65a` | `8678bf33429c2492f8ae39cfdba5bf34e92349e8e9c17fdec6f7f2c816b742a6` |
| `proofs/proof6/outage.py` | `ef7a7d458ec087b0294e7a374d1779f94ba65e7e` | `e2331de41cc91cf3e4e7d839554e5e8e27824bb873afd4521bbe761dd3af7462` |
| `proofs/proof6/proof_control.py` | `b7804bc59cfd67d3340f46e872d3ddd242e49383` | `775262f8925c9a4ada29d89d7e25bddbdbc7e8382e6a696374bb86e1d6dc6b40` |
| `proofs/proof6/proof_plan.json` | `c1d50e712e23607c0bab01e7af57dd724f9066bb` | `fd5af499de268003b8a8a6c9261b7708cb76b718e54222d105011c483711ff1c` |
| `proofs/proof6/reconcile.py` | `2d5d44a683c84b543b8bceb76307c918aad26239` | `22d9cc955f9e5780f334d8ab9437630107edca695c62fc238feb5e8fd3668b7e` |
| `proofs/proof6/ruleset_view.py` | `0e3905f2ffc2e1dcc011bc74743d0d8ae6cc2f2a` | `9ae91ea816971b4d2b4ec442ae8a703096f6531b16d5177b9e212cd5503fcaf4` |
| `proofs/proof6/writer.py` | `ab6c25a4f01f195f43d9dd67cd5d090058af2c09` | `b358f27e988902af112f634bb8775dcbd43ea0fd82d1db9bfd50791ff8c85964` |

### Normative per-case R3f selection

Fresh canaries include B01/B02 (meaningful ordinary direct rejection), C01 (normal gated positive transition), and C05/C11/D06/D07/D08/D09 (replay, permanent consumption, ambiguity and duplicate-send/admission safety). A01-A09/F01-F14/G01-G03/H01-H08 must freshly establish mutable security/identity and independent evidence review. These are mandatory minimum categories, never permission to omit the other table rows.

| Existing case | Evidence disposition | Reason category | Per-case rationale |
|---|---|---|---|
| `A01` | FRESH | FRESH_R3F | Fresh successor manifest, impact selection and recovery subconditions. |
| `A02` | FRESH | FRESH_R3F | Fresh immutable repository/owner binding. |
| `A03` | FRESH | FRESH_R3F | Fresh actual authority baseline; no reset to historical proposal state. |
| `A04` | FRESH | FRESH_R3F | Fresh authority/journal enforcement and canonical genesis protection. |
| `A05` | FRESH | FRESH_R3F | Fresh build/reducer/gate identities and full dependency inventory. |
| `A06` | FRESH | FRESH_R3F | Fresh non-admin writer grants; prior grants cannot attest current state. |
| `A07` | FRESH | FRESH_R3F | Fresh ordinary-client qualification and excluded privileged-role boundary. |
| `A08` | FRESH | FRESH_R3F | Fresh protected runtime, workflow, environment, permissions and concurrency. |
| `A09` | FRESH | FRESH_R3F | Fresh isolated secret binding/custody evidence without secret values. |
| `B01` | FRESH | FRESH_R3F | Fresh meaningful direct-push negative enforcement canary. |
| `B02` | FRESH | FRESH_R3F | Fresh direct Git-refs authority/journal negative enforcement canary. |
| `B03` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical Contents-API acceptance package established. |
| `B04` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical UI direct-edit acceptance package established; surface unavailability does not supply credit. |
| `B05` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical PR/merge bypass acceptance package established. |
| `B06` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical ChatGPT write-path acceptance package established. |
| `B07` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical Codex/local-Git write-path acceptance package established. |
| `B08` | FRESH | FRESH_R3F | Fresh force/rewrite protection for authority and journal. |
| `B09` | FRESH | FRESH_R3F | Fresh delete/recreation protection for authority and journal. |
| `B10` | FRESH | FRESH_R3F | Lookalike journal/authority cannot release new recovery admission. |
| `B11` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical unprotected-path probe acceptance package established. |
| `B12` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical CI-skipping bypass acceptance package established. |
| `C01` | FRESH | FRESH_R3F | Required fresh positive gated transition and post-recovery release. |
| `C02` | FRESH | FRESH_R3F | Gate rejection still reached safely through changed common admission. |
| `C03` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical malformed-proposal acceptance package established; parsing/validation can reject before recovery. |
| `C04` | FRESH | FRESH_R3F | Fresh-state rejection after legitimate later authority progression. |
| `C05` | FRESH | FRESH_R3F | Replay/old-candidate duplicate prevention across recovery. |
| `C06` | FRESH | FRESH_R3F | Recovery cannot introduce target/ref/record selection. |
| `C07` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical arbitrary-content input rejection package established; recovery coupling is not presumed. |
| `C08` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical caller-supplied parent/commit rejection package established; recovery coupling is not presumed. |
| `C09` | FRESH | FRESH_R3F | Fresh committed diff is exactly the new gate-produced candidate. |
| `C10` | FRESH | FRESH_R3F | Inspect changed internal journal/recovery surface for raw-write escape. |
| `C11` | FRESH | FRESH_R3F | Permanent consumption, job-token separation and resend resistance. |
| `D01` | FRESH | FRESH_R3F | Serialized admission and single-send permits across competing jobs. |
| `D02` | FRESH | FRESH_R3F | No-send disposition still valid under shared append/replay changes. |
| `D03` | FRESH | FRESH_R3F | Direct semantic change: confirmation loss then demonstrated recovery. |
| `D04` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical actual-process outage acceptance package established; sharing the recovery precheck is not behavioral-impact proof. |
| `D05` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical old-ALLOW rejection acceptance package established; recovery coupling is not presumed. |
| `D06` | FRESH | FRESH_R3F | Consumption survives failure; no old candidate/proof retry. |
| `D07` | FRESH | FRESH_R3F | Natural/lost-response ambiguity uses coupled journal/terminal paths. |
| `D08` | FRESH | FRESH_R3F | Direct change: exact terminal plus current-authority reconciliation. |
| `D09` | FRESH | FRESH_R3F | Direct change: fail-closed crash/partition/recovery admission. |
| `E01` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical local-state confusion acceptance package established. |
| `E02` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical alternate-branch authority-confusion acceptance package established. |
| `E03` | FRESH | FRESH_R3F | Detached/wrong-ancestry terminal must not release admission. |
| `E04` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical task-branch/PR authority-confusion acceptance package established. |
| `E05` | FRESH | NO_VALID_PRIOR_EVIDENCE_SO_FRESH | No qualified, independently reviewed historical default/runtime-branch authority-confusion acceptance package established. |
| `E06` | FRESH | FRESH_R3F | Stale authority read cannot release recovery or gate new work. |
| `F01` | FRESH | FRESH_R3F | Fresh ordinary inability to alter authority enforcement. |
| `F02` | FRESH | FRESH_R3F | Fresh writer inability to alter its enforcement boundary. |
| `F03` | FRESH | FRESH_R3F | Fresh before/after authority/journal protection continuity. |
| `F04` | FRESH | FRESH_R3F | Fresh source/build/gate/reducer stability across successor evidence. |
| `F05` | FRESH | FRESH_R3F | Fresh server enforcement, not skippable CI, supports the claim. |
| `F06` | FRESH | FRESH_R3F | Fresh push/merge permission consistency. |
| `F07` | FRESH | FRESH_R3F | Fresh credential isolation and fixed two-ref mutation boundary. |
| `F08` | FRESH | FRESH_R3F | Fresh ordinary alternate-credential bypass resistance. |
| `F09` | FRESH | FRESH_R3F | Fresh ordinary runtime-branch update/delete/rewrite resistance. |
| `F10` | FRESH | FRESH_R3F | Fresh App inability to replace protected runtime. |
| `F11` | FRESH | FRESH_R3F | Fresh branch/environment secret-release restriction. |
| `F12` | FRESH | FRESH_R3F | Fresh hosted runner and security-environment qualification. |
| `F13` | FRESH | FRESH_R3F | Fresh immutable external-action dependency pins. |
| `F14` | FRESH | FRESH_R3F | Fresh absence of secret exposure in successor evidence. |
| `G01` | FRESH | FRESH_R3F | Fresh prior bounded designation of privileged sessions. |
| `G02` | FRESH | FRESH_R3F | Fresh exclusion of privileged dispatch/proxy from normal evidence. |
| `G03` | FRESH | FRESH_R3F | Fresh interval has no excluded meta-authority protected-state mutation. |
| `H01` | FRESH | FRESH_R3F | Independent final ref/evidence reconstruction for new successor. |
| `H02` | FRESH | FRESH_R3F | Map every real advance to gate and ordered confirmed disposition. |
| `H03` | FRESH | FRESH_R3F | Fresh direct-write attempts all map to unchanged authority. |
| `H04` | FRESH | FRESH_R3F | Independently verify all 72 dispositions, per-case evidence/impact boundaries and justified escalation. |
| `H05` | FRESH | FRESH_R3F | Required fresh independent bypass attempt, not old authored probes. |
| `H06` | FRESH | FRESH_R3F | Fresh verification of synthetic-only scope and no pilot/adoption. |
| `H07` | FRESH | FRESH_R3F | Independent full journal, send/consumption/recovery/contradiction audit. |
| `H08` | FRESH | FRESH_R3F | Fresh independent runtime/workflow/environment identity verification. |

Inherited IDs: **none**. Inherited acceptance provenance: **none**. NOT_APPLICABLE IDs: **none**. Source/evidence identities above are provenance/impact references only. If an implementer cannot supply fresh evidence for a row, it remains BLOCKED/INCONCLUSIVE and prevents PASS; do not invent inheritance or remove the row.

For later successors, apply the paired contract's full eligibility conjunction before execution. Gate, transport, credential, journal, admission, shared-send, runtime/security and authority-protection changes require exact identity and direct/transitive impact analysis, affected-claim evidence, fresh mutable-security checks and independent approval. A shared function, common precheck or named subsystem alone is not a full-rerun trigger. Full fresh evidence is required when review cannot bound impact/prove unaffected isolation, the change actually couples all relevant claims, or no qualified inherited subset can be established from prior evidence. Missing, failed, unqualified or insufficient evidence makes the corresponding row fresh, never inherited. A future narrow successor may preserve qualified behavioral evidence only with independently proven non-interference and all eligibility requirements; mutable-security verification remains fresh. No implementer waiver or post-results case reduction is allowed. R3f remains 72 fresh under the table's distinct reasons. No delta exception repairs R3e D03 or changes an old verdict.

## Immediate FAIL conditions

Any of the following is an immediate Proof-6 FAIL:

1. an ordinary client advances, rewrites, or deletes the bound authority ref outside the gate-writer path;
2. the writer commits a gate-REJECTed, malformed, stale, or replayed mutation;
3. stale authorization can be reused after authority changes;
4. writer exposes arbitrary repository/ref/file/history/command/runtime/journal mutation, permits ordinary journal forgery, or treats journal as project-state authority;
5. force push or ref deletion is available to an ordinary client;
6. writer or ordinary AI/client identity can modify/disable its own authority-ref enforcement boundary;
7. writer credential or owner/meta-admin capability is available to ordinary operational ChatGPT/Codex/human/client execution, including by transfer from privileged setup; prior privileged designation does not exempt ordinary activity or weaken writer-key isolation;
8. an ordinary client or the writer App can substitute the frozen writer-runtime code/branch;
9. the App private key can be released to an alternate branch, self-hosted/local runner, ordinary-client-controlled workflow, log, artifact, or repository content;
10. implementation quietly changes which branch/ref counts as authority to make a test pass;
11. a configuration/build/gate/runtime identity changes during the frozen run and old/new results are combined; prospective eligibility between distinct frozen intervals never excuses within-interval drift;
12. an ambiguous possible-send outcome is reported unchanged/NOT_COMMITTED from old SHA alone or another unproven completion assumption;
13. authority send precedes any required PENDING or SEND_ARMED/equivalent confirmation, an unresolved or malformed/missing/conflicted journal obligation is ignored, or later authority mutation occurs before confirmed valid disposition; deleting Actions evidence or crashing must never refund consumption or erase an obligation;
14. a successful bypass is dismissed as “users normally would not do that”;
15. a failed ordinary case is excused through retrospective privileged relabeling, or privileged setup activity is counted as ordinary evidence/normal accepted transition; any privileged mutation during the accepted frozen interval invalidates it under G03, not an exception that permits PASS.

A helper/probe-only D04 outage cannot pass D04. Unavailable genuine process isolation is BLOCKED; counting it as actual writer isolation is false acceptance.

## False-PASS traps to reject explicitly

The following evidence is insufficient for PASS:

- “all tests passed” when tests only call the intended writer;
- a CLI wrapper that clients are expected to use voluntarily;
- local Git hooks;
- CI/status checks that can be skipped while direct ref mutation remains possible;
- CODEOWNERS/review policy without actual authority-write enforcement;
- path protection that leaves another commit/ref path capable of changing the same authority;
- a protected default branch while canonical state actually lives somewhere else;
- proving one of UI/API/CLI paths is blocked while another ordinary path remains writable;
- showing gate correctness without proving the gate is mandatory;
- a signed event/history scheme that does not also establish which signed head is the current authoritative one;
- assuming a task branch/PR/newer/default-branch commit is authority because it exists remotely;
- exercising owner break-glass and then counting the same frozen run as normal enforcement evidence;
- classifying an ordinary caller as privileged after its action, treating an entire AI product as privileged, or relying on a new session label while admin/writer credentials remain reachable;
- counting a privileged-session-triggered gated transition as an ordinary accepted transition;
- combining Revision-2 frozen evidence with Revision-3 acceptance instead of a new Revision-3 manifest/refreeze;
- sharing the writer credential with the client and calling the credential itself “the gate”;
- storing the App private key in local WSL/Docker/user storage where ordinary Codex/client administration can obtain it;
- putting the secret in a GitHub Actions environment without restricting deployment to the exact frozen protected runtime branch;
- using a self-hosted runner on the ordinary-client-controlled host and calling that credential isolation;
- leaving the runtime branch writable while treating the writer source hash as frozen;
- treating timeout/connection loss, old-SHA-only reads, runner exit or token expiry as proof that a delayed PATCH cannot still complete;
- using Actions-run discovery as safety membership, assuming two-ref atomicity, ignoring journal conflict, refunding failed consumption, or substituting a disconnected helper for actual writer-process isolation;
- retrying the same stale candidate after an ambiguous post-send outcome rather than reconciling the remote authority first.
- treating a terminal object's existence, journal PATCH success, local resolved flag or recovery entry as confirmed admission;
- skipping current-authority consistency for a terminal already parsed as resolved;
- using old case plans/setup receipts/test counts as evidence that an acceptance case ran;
- using a small diff, unchanged leaf hash or successful canary to waive a full-rerun trigger;
- applying prospective D03 case B to retrospectively award R3e PASS.

## Overall acceptance rule

Proof 6 is `PASS` only when every MANDATORY case is satisfied under its prospectively approved evidence disposition (all 72 fresh for R3f), frozen successor identities remained stable, every possible send has prior confirmed PENDING/SEND_ARMED, consumption survives failure/Actions retention, and every possible-send operation has a valid confirmed disposition under the durable model. No unresolved/inconsistent operation or credible ordinary-client bypass/duplicate/false transition may remain. Original INDETERMINATE invocation reports remain unchanged after recovery and do not by themselves block a prospectively exercised case whose required recovery actually passed. Every ordinary case requires fresh qualification; privileged sessions remain separately designated and read-only during the interval. A06/A09 cannot be waived.

The paired contract's prospective stopping rule requires all eleven safety conditions, fresh mutable-security checks and positive/negative canaries, plus independent review. Purely diagnostic, performance, observability, proof-reporting or safely recoverable liveness defects do not independently keep Phase 6 open unless they plausibly undermine safety. Missing mandatory safety evidence, unexercised recovery, unresolved authority or a known safety defect still prevent PASS; this rule grants no case-count exception.

A single known ordinary-client bypass is `FAIL`. Missing infrastructure is `BLOCKED`. Ambiguous evidence is `INCONCLUSIVE`. None may be promoted to PASS through prose judgment.

## Scope after PASS

A Proof-6 PASS would complete the six-proof synthetic foundation only. It would authorize at most the next owner decision about Phase 7 bounded real-project pilot design. It would not itself authorize migration, cross-project adoption, or parent control-plane DONE.
